package com.example.unitask_app.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA DE COLORES UNITASK ---

// Modo Claro
val PrimaryLight = Color(0xFF1A237E)    // Azul Índigo Profundo
val SecondaryLight = Color(0xFF00BFA5)  // Verde Acento
val BackgroundLight = Color(0xFFF5F6F7) // Gris muy claro (Fondo)
val SurfaceLight = Color(0xFFFFFFFF)    // Blanco (Tarjetas)
val OnBackgroundLight = Color(0xFF2D3436) // Gris Oxford (Texto Principal)
val OnSurfaceLight = Color(0xFF2D3436)    // Texto en tarjetas

// Modo Oscuro
val PrimaryDark = Color(0xFF9FA8DA)     // Azul suave (Acento en oscuro)
val SecondaryDark = Color(0xFF64FFDA)   // Verde agua
val BackgroundDark = Color(0xFF121212)  // Negro casi total (Fondo)
val SurfaceDark = Color(0xFF1E1E1E)     // Gris carbón (Tarjetas)
val OnBackgroundDark = Color(0xFFE1E1E1) // Gris muy claro (Texto en oscuro)
val OnSurfaceDark = Color(0xFFE1E1E1)    // Texto en tarjetas oscuro

val ErrorRed = Color(0xFFD32F2F)
